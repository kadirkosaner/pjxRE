/* ==========================================
   PHONE CONTACTS APP - Contact list with block
========================================== */

window.PhoneApps = window.PhoneApps || {};
window.PhoneApps.contacts = {
    render: function (vars) {
        return getContactListHtmlForContacts(vars);
    }
};
